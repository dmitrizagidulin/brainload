  <h2>Your Decks:</h2>
  <div class="btn" style="margin-bottom: 1em; clear: both;">
    <%= link_to image_tag("add.png") + " New Deck", new_card_deck_path() %>
  </div>

<% @user_categories.each do | category | %>
  <h3><%= category.name %></h3>
  <% category_decks = @user_decks_by_category[category.key].sort_by {|deck| deck.name } %>
  <ul id="card_deck_list">
    <% category_decks.each do |card_deck| %>
        <li class="card_deck_face">
          <span class="btn"><%= link_to 'Edit', edit_card_deck_path(card_deck) %></span>
          <span class="card_deck_face_title"><%=  link_to card_deck.name, card_deck %></span>
          <span class="card_deck_privacy_status <%= card_deck.deck_type %>_deck">(<%= card_deck.deck_type %>)</span>
          
          <span class="card_deck_face_description"><%=  card_deck.description %></span>
        </li>
    <% end %>
  </ul>
<% end %>
